services:
  postgres:
    image: postgres:16.4
    network_mode: host
    command: ["postgres", "-p", "55434"]
    environment:
      POSTGRES_DB: blueeconomy_intelligence
      POSTGRES_USER: blueeconomy
      POSTGRES_PASSWORD: local-only-integration-password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -p 55434 -U blueeconomy -d blueeconomy_intelligence"]
      interval: 2s
      timeout: 2s
      retries: 30
    tmpfs:
      - /var/lib/postgresql/data
